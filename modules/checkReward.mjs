import driverConfig from '../lib/driver.mjs'
import e from '../lib/events.mjs'

// function reward() {
//   e.once('reward', async (tel,password) => {
//    const driver = await driverConfig()
//    const mainInputWrapper = await driver.findElement(driver.listClass.By.className("content-body-card"))
//    const tel = await mainInputWrapper.findElement(driver.listClass.By.xpath("//input[@type='tel']")).sendKeys(tel)
//    const password = await mainInputWrapper.findElement(driver.listClass.By.xpath("//input[@type='password']")).sendKeys(password)
//   })
// }
// export default reward


 // тут надо написать с нуля !